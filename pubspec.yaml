name: smooth_star_rating_v2
description: A smooth rating bar that supports full and half rating with easy icon customizability. Forked from https://github.com/thangmam/smoothratingbar.git
version: 1.1.3
homepage: https://github:com/samu-developments/smoothratingbar.git

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter